// Although we don't have much in here now, as the project grows, 
// we might add in injection and other settings to our angular app, 
// which we would do here. This allows for separation of concerns 
// and makes maintaining the project easier
var app = angular.module('testApp', []);